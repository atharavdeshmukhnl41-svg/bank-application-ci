trigger:
  branches:
    include:
      - main
  paths:
    include:
      - services/wallet/**

pool:
  name: azureagent

variables:
  IMAGE_NAME: wallet
  ACR_NAME: bankdevacr
  ACR_LOGIN_SERVER: bankdevacr.azurecr.io
  IMAGE_TAG: $(Build.BuildId)

steps:
- checkout: self

- task: Docker@2
  displayName: Build & Push Wallet Image
  inputs:
    containerRegistry: payasyougo
    repository: $(ACR_LOGIN_SERVER)/$(IMAGE_NAME)
    command: buildAndPush
    dockerfile: services/wallet/Dockerfile
    buildContext: services/wallet
    tags: |
      $(IMAGE_TAG)
      latest
